# LFS assumes that the root file system (/) is of type ext4. To create an 
# ext4 file system on the LFS partition, run the following:
mkfs -v -t ext4 /dev/<xxx>

# Make swap partition
mkswap /dev/<yyy>

# Personally I used gparted. There is a few videos on youtube that'll teach you how to use it
